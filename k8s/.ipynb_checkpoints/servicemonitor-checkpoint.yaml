apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: chatbotai-metrics
  namespace: chatbotai
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: chatbotai
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
