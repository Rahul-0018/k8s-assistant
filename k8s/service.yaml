apiVersion: v1
kind: Service
metadata:
  name: chatbotai-service
  namespace: chatbotai
  labels:
    app: chatbotai
spec:
  selector:
    app: chatbotai
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  type: ClusterIP
